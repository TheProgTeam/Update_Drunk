using UnityEngine;
using System.Collections;
using Random = UnityEngine.Random;

public class DrunkardWalk : Floormaker {

	//The Four Possible Directions
	/*
	private Transform mytransform;
	public Transform  locWalls;
	static int CURRENT_CASE = 55;



	void Awake()
	{
		randtileAmount = Random.Range(3,5);
		Debug.Log (randtileAmount);




	}

	void DrunkWalk()
	{


		for(int x = 0;x<=randtileAmount;x++)
		{
			
			ChosenDir = Random.Range(0,3);
			Debug.Log(ChosenDir);
			
			switch (ChosenDir)
			{
				//WEST
			case 0:
				CURRENT_CASE +=10;
				Destroy(wallTiles[CURRENT_CASE]);
				break;
				//EAST
			case 1:
				CURRENT_CASE -=10;
				Destroy(wallTiles[CURRENT_CASE]);
				break;
				//NORTH
			case 2:
				CURRENT_CASE +=1;
				Destroy(wallTiles[CURRENT_CASE]);
				break;
				//SOUTH
			case 3:
				CURRENT_CASE -=1;
				Destroy(wallTiles[CURRENT_CASE]);
				break;	
				
				
				
				
			}
		}

	}



		


	// Use this for initialization
	void Start () {

		DrunkWalk();




	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	//instance1.transform.SetParent(boardHolder);
*/}

